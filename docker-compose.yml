version: '3'

services:
    node:
        container_name: lejeudelavie
        build:
            context: 'docker/node'
        volumes:
            - '.:/app'
        ports:
            - ${NODE_PORT}:${NODE_PORT}
        env_file:
            - .env
